import 'package:flutter/material.dart';
import 'package:fluapp/naviBottom.dart';

class Five extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Scaffold(
      body:
      Container(width: 100,height: 100,child: Text("Five"),),
    );
  }
}